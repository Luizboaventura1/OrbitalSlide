export default interface SliderOptions {
  loop?: boolean;       
  autoplay?: boolean;      
  autoplaySpeed?: number;  
  draggable?: boolean;     
  infinite?: boolean;     
  speed?: number;        
  navigation?: boolean;  
  pagination?: boolean; 
}
